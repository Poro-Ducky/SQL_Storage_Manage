<UserControl x:Class="QuanLyKho.Views.DangNhapView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="768" d:DesignWidth="1366" Background="Transparent">

    <Grid>
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#F3F6FD" Offset="0"/>
                <GradientStop Color="#D8E0F0" Offset="1"/>
            </LinearGradientBrush>
        </Grid.Background>

        <Border Width="420" Background="{StaticResource BgSurface}" CornerRadius="20" Padding="40,50" 
                HorizontalAlignment="Center" VerticalAlignment="Center">
            <Border.Effect>
                <DropShadowEffect BlurRadius="20" ShadowDepth="5" Opacity="0.05" Color="#131242"/>
            </Border.Effect>

            <StackPanel>
                <TextBlock Text="Hệ Thống Quản Lý" FontSize="24" FontWeight="Bold" Foreground="{StaticResource TextDark}" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                <TextBlock Text="Vui lòng đăng nhập để tiếp tục" FontSize="14" Foreground="{StaticResource TextMuted}" HorizontalAlignment="Center" Margin="0,0,0,30"/>

                <TextBlock Text="Tên đăng nhập" FontSize="13" FontWeight="SemiBold" Foreground="{StaticResource TextDark}" Margin="0,0,0,8"/>
                <Border Background="{StaticResource BgSurface}" CornerRadius="10" BorderBrush="{StaticResource BorderColor}" BorderThickness="1.5" Padding="12" Margin="0,0,0,20">
                    <TextBox Background="Transparent" 
                             BorderThickness="0" 
                             FontSize="14" 
                             Text="{Binding TenDangNhap, UpdateSourceTrigger=PropertyChanged, ValidatesOnNotifyDataErrors=True}"/>
                </Border>

                <TextBlock Text="Mật khẩu" FontSize="13" FontWeight="SemiBold" Foreground="{StaticResource TextDark}" Margin="0,0,0,8"/>
                <Border Background="{StaticResource BgSurface}" CornerRadius="10" BorderBrush="{StaticResource BorderColor}" BorderThickness="1.5" Padding="12" Margin="0,0,0,30">
                    <PasswordBox x:Name="txtPassword" Background="Transparent" BorderThickness="0" FontSize="14"/>
                </Border>

                <Button Command="{Binding LoginCommand}" CommandParameter="{Binding ElementName=txtPassword}" Background="{StaticResource PrimaryColor}" Foreground="White" FontWeight="Bold" FontSize="15" Padding="15" BorderThickness="0" Cursor="Hand">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="10"/>
                        </Style>
                    </Button.Resources>
                    <TextBlock Text="ĐĂNG NHẬP"/>
                </Button>

            </StackPanel>
        </Border>
    </Grid>
</UserControl>