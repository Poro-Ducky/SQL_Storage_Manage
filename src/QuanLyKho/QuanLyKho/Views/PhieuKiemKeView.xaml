<UserControl x:Class="QuanLyKho.Views.PhieuKiemKeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000" Background="Transparent">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#FAFBFC" CornerRadius="12" Padding="20" BorderBrush="{StaticResource BorderColor}" BorderThickness="1" Margin="0,0,0,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1*"/>
                    <ColumnDefinition Width="1*"/>
                    <ColumnDefinition Width="2*"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" Margin="0,0,15,0">
                    <TextBlock Text="Mã Phiếu Kiểm Kê" FontSize="12" FontWeight="SemiBold" Foreground="{StaticResource TextMuted}" Margin="0,0,0,6"/>
                    <TextBox Padding="10" BorderBrush="{StaticResource BorderColor}" BorderThickness="1.5" Text="{Binding MaPKK}" IsReadOnly="True" Background="#EEEEEE"/>
                </StackPanel>
                <StackPanel Grid.Column="1" Margin="0,0,15,0">
                    <TextBlock Text="Ngày Kiểm Kê" FontSize="12" FontWeight="SemiBold" Foreground="{StaticResource TextMuted}" Margin="0,0,0,6"/>
                    <DatePicker Padding="5" BorderBrush="{StaticResource BorderColor}" BorderThickness="1.5" SelectedDate="{Binding NgayLap}"/>
                </StackPanel>
                <StackPanel Grid.Column="2">
                    <TextBlock Text="Ghi chú tổng quát" FontSize="12" FontWeight="SemiBold" Foreground="{StaticResource TextMuted}" Margin="0,0,0,6"/>
                    <TextBox Padding="10" BorderBrush="{StaticResource BorderColor}" BorderThickness="1.5" Text="{Binding GhiChu, UpdateSourceTrigger=PropertyChanged}"/>
                </StackPanel>
            </Grid>
        </Border>

        <Grid Grid.Row="1" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0" Margin="0,0,15,0">
                <TextBlock Text="Chọn Sản Phẩm" FontSize="12" FontWeight="SemiBold" Foreground="{StaticResource TextMuted}" Margin="0,0,0,6"/>
                <ComboBox Padding="10" BorderBrush="{StaticResource BorderColor}" BorderThickness="1.5" ItemsSource="{Binding DanhSachSP}" DisplayMemberPath="TenSP" SelectedItem="{Binding SelectedSP}"/>
            </StackPanel>
            <StackPanel Grid.Column="1" Margin="0,0,15,0">
                <TextBlock Text="SL Hệ Thống" FontSize="12" FontWeight="SemiBold" Foreground="{StaticResource TextMuted}" Margin="0,0,0,6"/>
                <TextBox Padding="10" BorderBrush="{StaticResource BorderColor}" BorderThickness="1.5" IsReadOnly="True" Background="#EEEEEE" Text="{Binding SLHeThong}"/>
            </StackPanel>
            <StackPanel Grid.Column="2" Margin="0,0,15,0">
                <TextBlock Text="SL Thực Tế" FontSize="12" FontWeight="SemiBold" Foreground="{StaticResource TextMuted}" Margin="0,0,0,6"/>
                <TextBox Padding="10" BorderBrush="{StaticResource BorderColor}" BorderThickness="1.5" Text="{Binding SLThucTe, UpdateSourceTrigger=PropertyChanged}"/>
            </StackPanel>
            <StackPanel Grid.Column="3" Margin="0,0,15,0">
                <TextBlock Text="Lý do chênh lệch (Nếu có)" FontSize="12" FontWeight="SemiBold" Foreground="{StaticResource TextMuted}" Margin="0,0,0,6"/>
                <TextBox Padding="10" BorderBrush="{StaticResource BorderColor}" BorderThickness="1.5" Text="{Binding LyDo, UpdateSourceTrigger=PropertyChanged}"/>
            </StackPanel>
            <Button Command="{Binding AddDetailCommand}" Grid.Column="4" VerticalAlignment="Bottom" Background="{StaticResource WarningColor}" Foreground="White" FontWeight="Bold" Padding="25,12" BorderThickness="0" Cursor="Hand">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="8"/>
                    </Style>
                </Button.Resources>
                <TextBlock Text="+ THÊM KIỂM KÊ"/>
            </Button>
        </Grid>

        <Border Grid.Row="2" Background="{StaticResource BgSurface}" CornerRadius="12" BorderBrush="{StaticResource BorderColor}" BorderThickness="1">
            <DataGrid ItemsSource="{Binding DanhSachChiTiet}" AutoGenerateColumns="False" CanUserAddRows="False" Background="Transparent" BorderThickness="0" RowHeight="45">
                <DataGrid.Resources>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#F4F7FE"/>
                        <Setter Property="Foreground" Value="{StaticResource TextMuted}"/>
                        <Setter Property="FontWeight" Value="SemiBold"/>
                        <Setter Property="Padding" Value="15,10"/>
                    </Style>
                </DataGrid.Resources>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="SẢN PHẨM" Binding="{Binding TenSP}" Width="2.5*"/>
                    <DataGridTextColumn Header="SL HỆ THỐNG" Binding="{Binding SLHeThong}" Width="1.5*"/>
                    <DataGridTextColumn Header="SL THỰC TẾ" Binding="{Binding SLThucTe}" Width="1.5*"/>
                    <DataGridTextColumn Header="SỐ LỆCH" Binding="{Binding SLLech}" Width="1*"/>
                    <DataGridTextColumn Header="LÝ DO LỆCH" Binding="{Binding LyDo}" Width="2*"/>
                    <DataGridTemplateColumn Header="THAO TÁC" Width="1.2*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">

                                    <Button Command="{Binding DataContext.EditDetailCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" 
                        CommandParameter="{Binding}" Background="Transparent" BorderThickness="0" Cursor="Hand" Margin="0,0,15,0" ToolTip="Sửa dòng này">
                                        <TextBlock Text="&#xE70F;" FontFamily="Segoe MDL2 Assets" FontSize="16" Foreground="{StaticResource PrimaryColor}"/>
                                    </Button>

                                    <Button Command="{Binding DataContext.RemoveDetailCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" 
                        CommandParameter="{Binding}" Background="Transparent" BorderThickness="0" Cursor="Hand" ToolTip="Xóa dòng này">
                                        <TextBlock Text="&#xE74D;" FontFamily="Segoe MDL2 Assets" FontSize="16" Foreground="{StaticResource DangerColor}"/>
                                    </Button>

                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <Border Grid.Row="3" Margin="0,20,0,0" BorderBrush="{StaticResource BorderColor}" BorderThickness="0,2,0,0" Padding="0,20,0,0">
            <Button Command="{Binding SaveCommand}" HorizontalAlignment="Right" Background="{StaticResource PrimaryColor}" Foreground="White" FontWeight="Bold" Padding="40,15" FontSize="16" BorderThickness="0" Cursor="Hand">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="8"/>
                    </Style>
                </Button.Resources>
                <TextBlock Text="LƯU PHIẾU KIỂM KÊ"/>
            </Button>
        </Border>
    </Grid>
</UserControl>