<UserControl x:Class="QuanLyKho.Views.ThemDanhMucView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000" Background="Transparent">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="350"/>
            <ColumnDefinition Width="30"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="#FAFBFC" CornerRadius="12" Padding="25" BorderBrush="{StaticResource BorderColor}" BorderThickness="1">
            <StackPanel>
                <TextBlock Text="Thông Tin Danh Mục" FontSize="18" FontWeight="Bold" Foreground="{StaticResource TextDark}" Margin="0,0,0,25"/>

                <TextBlock Text="Mã Danh Mục (VD: L01)" FontSize="12" FontWeight="SemiBold" Foreground="{StaticResource TextMuted}" Margin="0,0,0,6"/>
                <TextBox Padding="12" BorderBrush="{StaticResource BorderColor}" BorderThickness="1.5"  Margin="0,0,0,15" Text="{Binding MaLoai, UpdateSourceTrigger=PropertyChanged}"/>

                <TextBlock Text="Tên Danh Mục (*)" FontSize="12" FontWeight="SemiBold" Foreground="{StaticResource TextMuted}" Margin="0,0,0,6"/>
                <TextBox Padding="12" BorderBrush="{StaticResource BorderColor}" BorderThickness="1.5"  Margin="0,0,0,15" Text="{Binding TenLoai, UpdateSourceTrigger=PropertyChanged}"/>

                <TextBlock Text="Ghi chú / Mô tả" FontSize="12" FontWeight="SemiBold" Foreground="{StaticResource TextMuted}" Margin="0,0,0,6"/>
                <TextBox Padding="12" BorderBrush="{StaticResource BorderColor}" BorderThickness="1.5"  Margin="0,0,0,25" Height="100" TextWrapping="Wrap" AcceptsReturn="True" Text="{Binding GhiChu}"/>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="10"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="10"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <Button Grid.Column="0" Command="{Binding AddCommand}" Background="{StaticResource SuccessColor}" Foreground="White" FontWeight="Bold" Padding="0,15" BorderThickness="0" Cursor="Hand">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="8"/>
                            </Style>
                        </Button.Resources>
                        <TextBlock Text="THÊM MỚI"/>
                    </Button>

                    <Button Grid.Column="2" Command="{Binding UpdateCommand}" Background="{StaticResource PrimaryColor}" Foreground="White" FontWeight="Bold" Padding="0,15" BorderThickness="0" Cursor="Hand">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="8"/>
                            </Style>
                        </Button.Resources>
                        <TextBlock Text="CẬP NHẬT"/>
                    </Button>

                    <Button Grid.Column="4" Command="{Binding ClearCommand}" Background="#E2E8F0" Foreground="{StaticResource TextDark}" FontWeight="Bold" Padding="15,15" BorderThickness="0" Cursor="Hand" ToolTip="Làm mới Form">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="8"/>
                            </Style>
                        </Button.Resources>
                        <TextBlock Text="&#xE72C;" FontFamily="Segoe MDL2 Assets" FontSize="16"/>
                    </Button>
                </Grid>
            </StackPanel>
        </Border>

        <Border Grid.Column="2" Background="{StaticResource BgSurface}" CornerRadius="12" BorderBrush="{StaticResource BorderColor}" BorderThickness="1">
            <DataGrid ItemsSource="{Binding DanhSachLoaiSP}" SelectedItem="{Binding SelectedLoai}" AutoGenerateColumns="False" CanUserAddRows="False" Background="Transparent" BorderThickness="0" RowHeight="45">
                <DataGrid.Resources>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#F4F7FE"/>
                        <Setter Property="Foreground" Value="{StaticResource TextMuted}"/>
                        <Setter Property="FontWeight" Value="SemiBold"/>
                        <Setter Property="Padding" Value="15,10"/>
                    </Style>
                </DataGrid.Resources>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="MÃ DM" Binding="{Binding MaLoai}" Width="0.8*"/>
                    <DataGridTextColumn Header="TÊN DANH MỤC" Binding="{Binding TenLoai}" Width="1.5*"/>
                    <DataGridTextColumn Header="GHI CHÚ" Binding="{Binding GhiChu}" Width="2*"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>