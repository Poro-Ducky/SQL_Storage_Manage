<UserControl x:Class="QuanLyKho.Views.DaiLyView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="768" d:DesignWidth="1100" Background="Transparent">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="350"/>
            <ColumnDefinition Width="30"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="#FAFBFC" CornerRadius="12" Padding="25" BorderBrush="{StaticResource BorderColor}" BorderThickness="1" VerticalAlignment="Top">
            <StackPanel>
                <TextBlock Text="Thông Tin Đại Lý / Khách Hàng" FontSize="18" FontWeight="Bold" Foreground="{StaticResource TextDark}" Margin="0,0,0,25"/>

                <TextBlock Text="Mã Đại Lý / Khách Hàng" FontSize="12" FontWeight="SemiBold" Foreground="{StaticResource TextMuted}" Margin="0,0,0,6"/>
                <Border Background="{StaticResource BgSurface}" CornerRadius="8" BorderBrush="{StaticResource BorderColor}" BorderThickness="1.5" Padding="10" Margin="0,0,0,15">
                    <TextBox Background="Transparent" BorderThickness="0" Foreground="{StaticResource TextDark}" Text="{Binding MaDL, UpdateSourceTrigger=PropertyChanged}" ToolTip="Nhập mã..."/>
                </Border>

                <TextBlock Text="Tên Đại Lý / Khách Hàng (*)" FontSize="12" FontWeight="SemiBold" Foreground="{StaticResource TextMuted}" Margin="0,0,0,6"/>
                <Border Background="{StaticResource BgSurface}" CornerRadius="8" BorderBrush="{StaticResource BorderColor}" BorderThickness="1.5" Padding="10" Margin="0,0,0,15">
                    <TextBox Background="Transparent" BorderThickness="0" Foreground="{StaticResource TextDark}" Text="{Binding TenDL, UpdateSourceTrigger=PropertyChanged}" ToolTip="Nhập tên..."/>
                </Border>

                <TextBlock Text="Số Điện Thoại" FontSize="12" FontWeight="SemiBold" Foreground="{StaticResource TextMuted}" Margin="0,0,0,6"/>
                <Border Background="{StaticResource BgSurface}" CornerRadius="8" BorderBrush="{StaticResource BorderColor}" BorderThickness="1.5" Padding="10" Margin="0,0,0,15">
                    <TextBox Background="Transparent" BorderThickness="0" Foreground="{StaticResource TextDark}" Text="{Binding SDT, UpdateSourceTrigger=PropertyChanged}"/>
                </Border>

                <TextBlock Text="Ghi chú / Mô tả" FontSize="12" FontWeight="SemiBold" Foreground="{StaticResource TextMuted}" Margin="0,0,0,6"/>
                <Border Background="{StaticResource BgSurface}" CornerRadius="8" BorderBrush="{StaticResource BorderColor}" BorderThickness="1.5" Padding="10" Margin="0,0,0,25">
                    <TextBox Background="Transparent" BorderThickness="0" Foreground="{StaticResource TextDark}" Height="80" TextWrapping="Wrap" AcceptsReturn="True" Text="{Binding DiaChi, UpdateSourceTrigger=PropertyChanged}"/>
                </Border>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="10"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="10"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <Button Grid.Column="0" Command="{Binding AddCommand}" Background="{StaticResource SuccessColor}" Foreground="White" FontWeight="Bold" Padding="0,15" BorderThickness="0" Cursor="Hand">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="8"/>
                            </Style>
                        </Button.Resources>
                        <TextBlock Text="THÊM MỚI"/>
                    </Button>

                    <Button Grid.Column="2" Command="{Binding UpdateCommand}" Background="{StaticResource PrimaryColor}" Foreground="White" FontWeight="Bold" Padding="0,15" BorderThickness="0" Cursor="Hand">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="8"/>
                            </Style>
                        </Button.Resources>
                        <TextBlock Text="CẬP NHẬT"/>
                    </Button>

                    <Button Grid.Column="4" Command="{Binding ClearCommand}" Background="#E2E8F0" Foreground="{StaticResource TextDark}" FontWeight="Bold" Padding="15,15" BorderThickness="0" Cursor="Hand" ToolTip="Làm mới Form">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="8"/>
                            </Style>
                        </Button.Resources>
                        <TextBlock Text="&#xE72C;" FontFamily="Segoe MDL2 Assets" FontSize="16"/>
                    </Button>
                </Grid>
            </StackPanel>
        </Border>

        <Border Grid.Column="2" Background="{StaticResource BgSurface}" CornerRadius="12" BorderBrush="{StaticResource BorderColor}" BorderThickness="1">
            <DataGrid ItemsSource="{Binding DanhSachDaiLy}" SelectedItem="{Binding SelectedDaiLy}" AutoGenerateColumns="False" CanUserAddRows="False" Background="Transparent" BorderThickness="0" RowHeight="45">
                <DataGrid.Resources>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#F4F7FE"/>
                        <Setter Property="Foreground" Value="{StaticResource TextMuted}"/>
                        <Setter Property="FontWeight" Value="SemiBold"/>
                        <Setter Property="Padding" Value="15,10"/>
                    </Style>
                </DataGrid.Resources>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="MÃ" Binding="{Binding MaDL}" Width="0.8*"/>
                    <DataGridTextColumn Header="TÊN" Binding="{Binding TenDL}" Width="2*"/>
                    <DataGridTextColumn Header="CHI TIẾT" Binding="{Binding DiaChi}" Width="2*"/>

                    <DataGridTemplateColumn Header="THAO TÁC" Width="0.8*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Command="{Binding DataContext.DeleteCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}" Background="Transparent" BorderThickness="0" Cursor="Hand" HorizontalAlignment="Right" Margin="0,0,15,0">
                                    <TextBlock Text="&#xE74D;" FontFamily="Segoe MDL2 Assets" FontSize="16" Foreground="{StaticResource DangerColor}"/>
                                </Button>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>